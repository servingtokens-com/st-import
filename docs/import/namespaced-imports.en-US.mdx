---
title: React Hooks for Data Fetching
---

import Callout from 'nextra-theme-docs/callout'
import Features from 'components/features'
import Bleed from 'nextra-theme-docs/bleed'
import CustomLink from 'components/CustomLink'

{
// wrapped with {} to mark it as javascript so mdx will not put it under a p tag
}

{<h1 className="inline-block pb-0 text-transparent bg-clip-text bg-gradient-to-r from-white to-teal-300">Namespaced Imports</h1>}

<Callout emoji="✅">
 Creating an account is <strong>easy</strong>, and requires no <strong>card</strong> to <CustomLink href="http://localhost:3333/auth/register">register</CustomLink>.
</Callout>

Any import where the beginning portion (up to the first slash) of the URL does not contain a . is considered a namespaced import.

A namespaced import means that the IMPORT_SERVER (which defaults to https://import.sh) is prepended to the import URL. For example, these two import invocations are identical:
 (unless the `IMPORT_RELOAD=1` environment variable is set).

 ### Examples

- `import "assert"`

- `import "https://import.sh/assert"`

Let's take a look at importing this tootallnate/hello "Hello World" import from GitHub:

```bash
#!/usr/bin/env import
import "tootallnate/hello"

hello
# Hello, from @TooTallNate!
```

## The import.sh server

The default `IMPORT_SERVER` is https://import.sh, which serves GitHub repositories that are "import-compatible", according to the following conventions:

The main import syntax is `import <org>/<repo>`

The entry point of the import is the file with the name of the repo with a .sh suffix
If there is no / in the import path, then the default org (importpw) is applied
Specific tags may be referenced by appending an at `@version>` to the end.

## Top-level imports

The importpw GitHub organization houses the top-level namespace imports. A top-level import happens when there is no `/` in the import path.

For example, the `assert` module includes functions that write simple unit testing scripts:

```bash
#!/usr/bin/env import
import "assert"

assert 1 = 2
# assertion failed: 1 = 2
```

Here are some useful top-level imports:

<CustomLink href="https://import.sh/array">array</CustomLink>
<CustomLink href="https://import.sh/assert">assert</CustomLink>
<CustomLink href="https://import.sh/confirm">confirm</CustomLink>
<CustomLink href="https://import.sh/dns">dns</CustomLink>
<CustomLink href="https://import.sh/emitter">emitter</CustomLink>
<CustomLink href="https://import.sh/http">http</CustomLink>
<CustomLink href="https://import.sh/os">os</CustomLink>
<CustomLink href="https://import.sh/path">path</CustomLink>
<CustomLink href="https://import.sh/prompt">prompt</CustomLink>
<CustomLink href="https://import.sh/querystring">querystring</CustomLink>
<CustomLink href="https://import.sh/string">string</CustomLink>
<CustomLink href="https://import.sh/tcp">tcp</CustomLink>

See the <CustomLink href="https://github.com/importpw">importpw</CustomLink> org on GitHub for the complete listing of repositories.

## Community imports

Here are some GitHub repositories that are known to be compatible with `import`:

<CustomLink href="https://import.sh/tootallnate/nexec">tootallnate/nexec</CustomLink>
<CustomLink href="https://import.sh/kward/log4sh">kward/log4sh</CustomLink>
<CustomLink href="https://import.sh/kward/shflags">kward/shflags</CustomLink>
<CustomLink href="https://import.sh/kward/shunit2">kward/shunit2</CustomLink>
<CustomLink href="https://import.sh/robwhitby/shakedown">robwhitby/shakedown</CustomLink>

Send a <CustomLink href="https://github.com/importpw/import/pulls">pull request</CustomLink> if you would like to have an import listed here